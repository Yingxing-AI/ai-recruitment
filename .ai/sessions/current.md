# Current Session

## 候选事件

说明：开发过程中只收集候选事件，结束工作时直接归档到 Session Archive，不写入长期记忆。

### ADR 候选

- Docker Compose 依赖服务访问路径已改为宿主机网关，并增加后端数据库启动重试。

### 里程碑候选

- 本地产品界面已恢复可访问，`backend` 健康接口和仪表盘摘要接口验证通过。
- 本地前端临时入口 `http://localhost:3001` 已恢复页面访问，绕过旧 Docker `frontend` 容器代理故障。

### 风险候选

- Docker 中仍残留一个重名 `worker` 容器，后续重建 worker 时需要清理。
- Docker 守护进程当前拒绝停止旧 `frontend` 容器，导致 `3000` 端口仍可能命中旧代理配置。

### 知识候选

- 当前运行环境存在“容器间 DNS 可解析但 TCP 不通”的现象，可通过宿主机网关和端口映射规避。
