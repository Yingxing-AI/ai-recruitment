# Session Archive

日期：2026-06-22

## 20:00

## 今日关键进展
- 修复本地后端启动阻塞：应用启动阶段增加数据库重试，Docker Compose 下改为通过宿主机网关访问 PostgreSQL、Redis 和 MinIO。
- 修复前端页面转圈：确认旧 Docker `frontend` 容器反代 `backend:8000` 超时，前端代理配置已改为通过宿主机网关访问后端。
- 恢复正式产品入口：`http://localhost:3000` 已恢复页面与数据访问，`http://localhost:8000` 健康检查和核心业务接口验证通过。

## 新增或确认的 ADR
- 新增 ADR 0004：Docker Compose 依赖服务走宿主机网关。

## 风险变化
- 当前 Docker 守护进程仍存在“不能正常停止/替换旧容器”的环境特性，后续重建 `frontend` / `worker` 时仍可能遇到容器残留或命名冲突。

## 范围变化
- 暂无。

## 重要候选事项
- 当前运行环境存在“容器间 DNS 可解析但 TCP 不通”的现象，可通过宿主机网关和端口映射规避。
