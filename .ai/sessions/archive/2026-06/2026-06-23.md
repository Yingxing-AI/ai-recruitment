# Session Archive

日期：2026-06-23

## 今日关键进展
- 完成本地前端开发链路收口：Vite 默认端口调整为 `3001`，并同步修正文档与后端默认 CORS，消除了与 Docker 正式入口 `3000` 的端口冲突。
- 恢复并验证 Docker 正式入口：重新拉起 Compose 栈，确认 `http://localhost:3000` 和 `http://localhost:8000/api/v1/health` 均可正常访问。
- 完成 `worker` 容器命名漂移排查：确认 `f384d9dbc1a7_ai-recruitment-worker-1` 属于当前 Compose 项目的历史替换残留。

## 新增或确认的 ADR
- 无。

## 风险变化
- `worker` 容器命名漂移仍未根治；尝试单独重建 `worker` 时，Docker daemon 对停止旧容器返回 `permission denied`。

## 范围变化
- 无。

## 重要候选事项
- 当前 MVP 正式访问链路已恢复稳定，但 Docker 环境治理仍受宿主机容器控制权限影响。
